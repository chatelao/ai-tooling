version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_build:
      - python scripts/generate_mkdocs_config.py

python:
  install:
    - requirements: docs-requirements.txt

mkdocs:
  configuration: mkdocs.yml
