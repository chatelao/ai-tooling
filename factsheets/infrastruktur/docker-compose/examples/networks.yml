version: '3.8'
services:
  web:
    image: nginx:alpine
    networks:
      - frontend
      - backend
  db:
    image: redis:alpine
    networks:
      - backend

networks:
  frontend:
  backend:
    internal: true
