@if (User.Identity.IsAuthenticated)
{
    <p>Welcome back, @User.Identity.Name!</p>
}
else
{
    <p>Please log in.</p>
}

@switch (Model.Status)
{
    case Status.Active:
        <span>Active</span>
        break;
    default:
        <span>Inactive</span>
        break;
}
