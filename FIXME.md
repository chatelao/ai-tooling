# FIXME

## Group 650e7cab (Bioinformatik & CAD-3D)

- **FreeCAD**: Headless verification via `xvfb-run` shows `libEGL warning: DRI3 error: Could not get DRI3 device`, though it seems to start. (Updated validation to filter warning)
